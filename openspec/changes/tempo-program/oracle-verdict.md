# Verdict — независимая проверка плана (gate G2)

**ACCEPT** — план принят с обязательными правками, и все они внесены до старта работ.

## Кто проверял

Независимый агент-ревьюер. Ему дали бриф пользователя (12 пунктов дословно), описание референса
и спецификации волн 0–1 — **без манифеста**. Он читал только `proposal.md`, `tasks.md`,
`interfaces.md`, `specs/**` и `recon.md`, то есть не мог сверить план с чек-листом автора.

## Что найдено и что сделано

| Приоритет | Находка | Правка в плане |
|---|---|---|
| 1 | Поиск (волна 2) создаётся раньше сущностей (волны 3, 5, 7) — инверсия зависимостей: триггеры и контракты пришлось бы переписывать каждую волну | R39 + реестр источников `registerSearchSource` (`interfaces.md` §8); задачи 2.0, 3.8, 5.4 |
| 1 | Односложные хоткеи референса (N, T, L, D, R, S, B, C, ?) срабатывали бы при наборе текста | R38 + слой шорткатов с подавлением в `input`/`textarea`/`contenteditable` (`interfaces.md` §7); задача 0.12 |
| 1 | Нет сценария первого запуска: разрешения на микрофон и захват экрана, загрузка модели в сотни МБ, отказ и отсутствие сети | R45; задачи 7.6, 11.9 |
| 1 | Медиа пишут файлы мимо БД: нет потолка хранилища, ротации и удаления файла вместе с записью | R43 + жизненный цикл медиа (`interfaces.md` §9); задачи 6.4, 7.5, 10.9 |
| 2 | Секция Task Rollover из референса (с часовым поясом) отсутствовала | R40 + `specs/daily-planning/spec.md`; задача 4.4 |
| 2 | Локализация RU/EN и история чата копилота терялись при переезде на SQLite | R42, R44; задачи 0.13, 0.14 |
| 3 | Focus Audio из референса пропущен (пользователь отменил только Apple Music) | R41 + спека помодоро; задача 10.8 |
| 3 | «Не нагружает GPU» без числа — требование непроверяемо | R46 + бюджеты производительности (`interfaces.md` §10) |

## Повторная сверка после правок

- `openspec validate tempo-program` — валиден.
- Трассируемость: 46 требований, **0** без задачи; каждая задача ссылается на R##.
- Все четыре находки первого приоритета закрыты; находки второго и третьего приоритета закрыты
  тем же проходом (пользователь подтвердил RU/EN и генерируемые шумы).

## Что осталось открытым (честно)

1. **Google Cloud проект** — создаёт пользователь; без него волна 3 не принимается.
2. **Коллизии имени Tempo** — не проверены: инструмент веб-поиска недоступен без подписки.
   Техническое переименование от публичного имени не зависит.
3. **macOS/Linux** — сознательно вне программы (Windows-first, решение пользователя).
