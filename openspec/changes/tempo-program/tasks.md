# Tasks: Tempo Program

Порядок обязателен. R## — из `manifest.md`. Волна = релиз (R35).

## Волна 0. Фундамент (R01, R02, R29, R30, R31, R32, R33, R34)

- [ ] 0.1 Дизайн-токены в `src/constants/design.ts`: палитра (почти чёрный фон, три уровня поверхностей,
      три уровня текста), шрифты (Inter + JetBrains Mono), шкала размеров, радиусы, отступы (4/8/12/16/24/32)
- [ ] 0.2 Компоненты дизайн-системы: `Row` (подпись + описание слева, контрол справа), `Toggle`, `Segmented`,
      `Slider` (с подписями min/max), `SectionHeader`, `Kbd`, `Divider`, `IconButton`
- [ ] 0.3 Новый каркас `src/App.tsx`: сайдбар-навигация как в референсе (иконка + подпись, активная пилюля),
      сворачивание (⌘S), роутер экранов, `TitleBar` остаётся (drag, пин, мини-оверлей)
- [ ] 0.4 Акцентный цвет: палитра из 8 оттенков в настройках, применяется через CSS-переменную;
      шесть прежних тем заменяются мостом `themeFromTokens` (interfaces §11) — экраны волн 1–12
      снимают проп `theme` по мере переписывания
- [ ] 0.5 SQLite на `rusqlite` (bundled, проверено: сборка 12 с, FTS5 с `unicode61` находит русский
      текст) + миграции; `data/tempo.db` рядом с exe в portable-режиме, `%APPDATA%` в установке;
      весь SQL живёт в Rust, белый список таблиц — `storage/schema.rs` (interfaces §2)
- [ ] 0.6 `src/services/db.ts` — типизированные репозитории поверх Rust-команд, **без SQL-строк**;
      `updated_at`/`deleted_at` ставит Rust; плюс `src/services/settings.ts` поверх `preferences`
- [ ] 0.7 Перенос данных: при отсутствии `tempo.db` читаем `alarmer.json` — будильники, задачи, заметки,
      настройки переносим; программы и направления отбрасываем (R30, R31).
      Экран будильников, задач, заметок и настроек переключается на БД в этой же волне: иначе правки
      волн 1–2 уйдут в JSON мимо БД и потеряются к волне 3.
      Мост: `StoreService` сохраняет свой публичный API, но его бэкендом становится БД — экраны
      переходят на `repo()` в своих волнах, когда их всё равно переписывают. Мост удаляется в волне,
      где исчезает последний потребитель (задача 12.x), а не раньше
- [ ] 0.8 Удаление: `SchedulesPanel`, `BlockPlayer`, `DirectionEditor`, `JournalView`, `scheduleParser`,
      `scheduleEngine`, `focusBudget`, `TodayView` + их тесты; `scheduler.rs` теряет раскрытие программ
- [ ] 0.9 Переименование: `productName` Tempo, `identifier` app.tempo.desktop, `tempo.exe`,
      URL манифеста в трёх местах, иконки
- [ ] 0.10 CI: Windows-only матрица; portable-архив `tempo-v<tag>-portable-windows.zip` + `tempo-portable.exe`
- [ ] 0.11 Проверить автообновление на живой portable-копии (R33) — пользователь подтверждает
- [ ] 0.12 Слой горячих клавиш (R38): реестр `registerShortcut`, односложные клавиши не срабатывают
       в `input`/`textarea`/`contenteditable`, экран Shortcuts строится из реестра
- [ ] 0.13 Язык интерфейса (R42): новые строки в обоих языках, переключатель RU/EN в шапке работает
- [ ] 0.14 Импорт истории чата копилота в БД (R44) — часть мигратора 0.7, отдельного кода не требует
- [ ] 0.15 `bun run check:all` зелёный, clippy чист, ratchet покрытия не сломан

## Волна 1. Помодоро на главном + фон (R03, R04, R05, R37)

- [ ] 1.1 Rust: режимы таймера `pomodoro` и `stopwatch` вместо `countdown/flow/block`
- [ ] 1.2 Rust: цикл помодоро — фокус/перерыв, длинный перерыв после 4, автостарт следующей фазы
- [ ] 1.3 Rust: запись сессии в SQLite (`sessions`: вид, начало, конец, длительность, завершена)
- [ ] 1.4 Главный экран: крупный циферблат, номер помодоро в цикле, кнопки старт/пауза/сброс
- [ ] 1.5 Настройки помодоро: ползунок длительности 10–120, длина перерыва, длинного перерыва, автостарт
- [ ] 1.6 Динамический фон: живой градиент, реагирующий на фазу и время суток; отключаемый; без GPU-нагрузки в покое
- [ ] 1.7 Мини-оверлей переведён на помодоро (фаза, время, прогресс)
- [ ] 1.8 Focus Audio (R41): `src/services/focusAudio.ts` — встроенные шумы, генерируемые в WebAudio;
      играет в фокусе, молчит на перерыве, выбор сохраняется. Строка выбора — в настройках помодоро
- [ ] 1.9 Тесты: цикл (4-й помодоро даёт длинный перерыв), пауза не сбрасывает фазу, фон меняется по фазе
- [ ] 1.10 Релиз 0.5.0

## Волна 2. Поиск (R06)

- [ ] 2.0 Реестр поиска (R39): `registerSearchSource` в JS отвечает за представление результата,
       индексатор один и живёт в Rust — волна-владелец сущности добавляет свой `kind` в
       `repo::reindex_fts` и зовёт `reindex(kind)` после изменений (см. interfaces §8)
- [ ] 2.1 Rust/SQL: наполнение `search_fts` для будильников и сессий, санирование запроса FTS5
       (спецсимволы и префиксный поиск), `reindex` при старте в `dbReady`
- [ ] 2.2 Палитра Cmd+K: ввод, группы результатов, клавиатурная навигация, Esc
- [ ] 2.3 Реестр команд: старт/пауза помодоро, новая задача, новая заметка, смена фона, переходы по экранам
- [ ] 2.4 Контентный поиск с переходом к найденному и подсветкой совпадения
- [ ] 2.5 Тесты: пустой запрос, только команды, только контент, спецсимволы FTS, рейтинг
- [ ] 2.6 Релиз 0.6.0

## Волна 3. Задачи, списки, Google Calendar (R07, R08, R09, R10, R11)

- [ ] 3.1 Задачи: CRUD, дата/время, приоритет, подзадачи, завершение, сортировка, пустые состояния
- [ ] 3.2 Списки: CRUD простых чек-листов, перенос пункта в задачу
- [ ] 3.3 OAuth Google: loopback + PKCE, refresh-токен в keyring, отключение, статус
- [ ] 3.4 Чтение календаря: полная и инкрементальная синхронизация по `syncToken`, обработка `410 Gone`, опрос
- [ ] 3.5 Зеркало событий в `events`; показ в списке задач и в Daily Planning
- [ ] 3.6 Запись задач в Google: создание/обновление/удаление события для задачи со временем
- [ ] 3.7 Тесты: токен истёк, сеть упала, событие удалено в Google, конфликт времени, отключённый аккаунт
- [ ] 3.8 Регистрация источника поиска для задач и списков (R39)
- [ ] 3.9 Релиз 0.7.0

## Волна 4. Daily Planning (R12)

- [ ] 4.1 Экран дня: таймлайн со слотами, события Google и задачи
- [ ] 4.2 Утренний ритуал: выбрать главное, разложить по слотам
- [ ] 4.3 Вечерний обзор: сделанное, незавершённое, перенос на завтра
- [ ] 4.4 Task Rollover (R40): перенос незавершённых после заданного часа, очистка устаревших слотов,
       часовой пояс в настройках; выключен по умолчанию, пока пользователь не включит
- [ ] 4.5 Тесты: пустой день, перенос через границу суток, смена часового пояса, rollover выключен
- [ ] 4.6 Релиз 0.8.0

## Волна 5. Notes (R13)

- [ ] 5.1 Список заметок, редактор markdown, предпросмотр, закрепление
- [ ] 5.2 `[[ссылки]]`: автодополнение, создание при отсутствии цели, панель «Упоминается в»
- [ ] 5.3 Прикрепление задач/записей/рисунков к заметке через `links`
- [ ] 5.4 Регистрация источника поиска для заметок (R39)
- [ ] 5.5 Тесты: битая ссылка, переименование цели, цикл ссылок, обратные ссылки после удаления
- [ ] 5.6 Релиз 0.9.0

## Волна 6. Drawings (R14)

- [ ] 6.1 Бесконечный холст: панорама, зум, векторные штрихи
- [ ] 6.2 Инструменты: перо (давление), маркер, ластик, фигуры, текст
- [ ] 6.3 Экспорт PNG/SVG, сохранение в `drawings` + файл превью в `data/assets`
- [ ] 6.4 Жизненный цикл медиа для рисунков (R43): удаление убирает файл, место видно в настройках
- [ ] 6.5 Производительность холста (R46): 60 к/с при 500 штрихах — замер и порог в тесте
- [ ] 6.6 Тесты: сохранение/загрузка холста, экспорт, удаление файла вместе с записью
- [ ] 6.7 Релиз 0.10.0

## Волна 7. Recordings: звук и экран (R15, R16)

- [ ] 7.1 Запись микрофона (cpal), индикатор уровня, пауза
- [ ] 7.2 Системный звук: WASAPI loopback
- [ ] 7.3 Запись экрана: выбор монитора или окна, кодирование в файл, превью кадра
- [ ] 7.4 Библиотека записей: длительность, размер, воспроизведение, переименование, удаление
- [ ] 7.5 Жизненный цикл медиа и потолок хранилища (R43): удаление убирает файл, лимит с очисткой,
       занятое место в настройках
- [ ] 7.6 Онбординг разрешений (R45): запрос микрофона и захвата экрана, отказ обрабатывается явно
- [ ] 7.7 Производительность записи (R46): ≤15% CPU при записи экрана — замер
- [ ] 7.8 Тесты: старт/стоп без потери файла, отмена, нет устройств, нет разрешения, переполнение квоты
- [ ] 7.9 Релиз 0.11.0

## Волна 8. Calendar (R18)

- [ ] 8.1 Виды месяц / неделя / день, навигация, «сегодня»
- [ ] 8.2 Локальные события и зеркало Google в одном виде, разные акценты источника
- [ ] 8.3 Создание и правка события по клику на слоте; задача как событие
- [ ] 8.4 Тесты: переходы месяцев, события за полночь, многодневные, вседневные
- [ ] 8.5 Релиз 0.12.0

## Волна 9. Statistics (R19)

- [ ] 9.1 Метрики: часы фокуса по дням/неделям, число помодоро, доля выполненных задач
- [ ] 9.2 Серия дней, тепловая карта, лучшие часы дня
- [ ] 9.3 Тесты: границы недели (понедельник), пустые дни, часовые пояса
- [ ] 9.4 Релиз 0.13.0

## Волна 10. Settings (R20, R21, R25)

- [ ] 10.1 Раздел General: режим таймера по умолчанию, длительности, звук конца, фон, акцент
- [ ] 10.2 Раздел Integrations: Google Calendar (Connect/Disconnect/статус); Apple Music отсутствует (R21)
- [ ] 10.3 Раздел Speech to Text: переключатель, выбор хоткея, выбор модели — каркас под волну 11
- [ ] 10.4 Раздел Shortcuts: список горячих клавиш как в референсе, включая Toggle Sidebar
- [ ] 10.5 Раздел About & Account: версия, аккаунт Google, проверка обновлений, portable-статус
- [ ] 10.6 Тесты: сохранение настроек, миграция старых значений, переключение акцента
- [ ] 10.7 Копилот (R36): научить создавать задачи, списки и заметки, собирать план дня из текста
       и отвечать по задачам/плану; прежние умения про программы и направления удалить
- [ ] 10.8 Focus Audio (R41) — сделано в волне 1 (1.8); здесь только проверка, что строка живёт в новых настройках
- [ ] 10.9 Строка Task Rollover и часовой пояс в General (R40); потолок хранилища и занятое место (R43)
- [ ] 10.10 Тесты: копилот не предлагает удалённые сущности, ошибка модели не ломает UI
- [ ] 10.11 Релиз 0.14.0

## Волна 11. Speech to Text (R22, R23, R24, R17)

- [ ] 11.1 Rust: движок на крейтах Handy (`transcribe-cpp`; при необходимости `transcribe-rs`) — обёртка над моделью
- [ ] 11.2 Загрузка моделей: каталог (tiny/base/small/medium), выбор при первом включении, прогресс, зеркало, отмена
- [ ] 11.3 Захват: `cpal` → 16 кГц, VAD с обрезкой тишины, порог уттерэнса
- [ ] 11.4 Хоткей: `handy-keys` (или global-shortcut), режимы push-to-talk и toggle, индикатор записи
- [ ] 11.5 Вставка текста: `SendInput` + сохранение/восстановление буфера; fallback «скопировать»
- [ ] 11.6 Облачный путь: тот же BYOK-ключ копилота, переключатель «локально/облако»
- [ ] 11.7 Транскрипция записей + backfill существующих + индексация транскриптов в поиск (R17)
- [ ] 11.8 Копилот читает транскрипты и отвечает по записям (R36 + R17)
- [ ] 11.9 Онбординг модели (R45): выбор модели при первом включении, загрузка с прогрессом и отменой,
       поведение без сети и при нехватке места на диске
- [ ] 11.10 Производительность фона (R46): ≤2% CPU в покое на главном экране — замер
- [ ] 11.11 Тесты: пустая запись, нет модели, нет разрешения на микрофон, вставка в elevated-окно (ограничение)
- [ ] 11.12 Релиз 0.15.0

## Волна 12. Аккаунт и синхронизация (R26, R27)

- [ ] 12.1 Scope `drive.file`, подключение Drive рядом с Calendar, статус и отключение
- [ ] 12.2 `sync_outbox`: журнал изменений всех синхронизируемых таблиц
- [ ] 12.3 Транспорт: выгрузка/загрузка журнала в скрытую папку приложения в Drive, разрешение по `updated_at`
- [ ] 12.4 Флаг «синхронизировать медиа»; медиа едет файлами, без попадания в БД
- [ ] 12.5 Тесты: два устройства, конфликт правок, офлайн-правки, удалённый объект воскрешается корректно
- [ ] 12.6 Релиз 0.16.0

## После программы

- [ ] PWA (R28, deferred): браузерный OAuth, хранилище в браузере, отказ от нативных частей
