# Tempo — состояние программы (handoff)

**Этот файл — точка возобновления.** Если сессия прервалась, начните отсюда:
прочитайте раздел «Где мы сейчас», затем `openspec/changes/tempo-program/tasks.md`
(галочки по волнам) и `git log --oneline master..feat/wave0-foundation`.

Обновлено: 2026-09-20 · ветка `feat/wave0-foundation` · **версия 0.8.0** · 335 vitest + 77 cargo

## Решения Wave 0 (30 вопросов, 4 раунда — все отвечены)

Полный редизайн + 12 фич. Полное переименование **Alarmer → Tempo** (identifier
`app.tempo.desktop`, `tempo.exe`): одна ручная переустановка, дальше автообновление работает.

Порядок: **шаг 0 (фундамент) → строго 1→12**, каждая фича своим релизом (0.4.0 … 0.16.0).

| Решение | Выбор пользователя |
|---|---|
| Будильники | остаются; программы-расписания удаляются без миграции |
| Хранилище | SQLite (rusqlite), portable-режим рядом с exe |
| Аккаунт | Google auth + синк через личный Google Drive |
| Динамический фон | живой градиент по фазе таймера и времени суток |
| Speech to Text | локальный Whisper по умолчанию + облако опцией; выбор модели на первом старте |
| Поиск | команды + навигация + контент в одной палитре |
| Google Calendar | чтение событий + запись задач событиями |
| Daily Planning | ритуал план → день → обзор |
| Модули | Notes/Drawings/Recordings отдельно + ссылки, плюс Lists |
| Темы | один стиль + настраиваемый акцент |
| Статистика | время, помодоро, задачи, ритм |
| Копилот | остаётся, переучивается на новые сущности |
| Записи | микрофон + системный звук + экран (монитор/окно), без камеры |
| Рисунки | бесконечный холст |
| Помодоро | классические 25/5 + стоп-вотч |
| Платформы | Windows-first (macOS/Linux из CI убираются) |
| Язык | RU/EN сохраняем |
| Focus Audio | генерируемые шумы + существующая YouTube-ссылка; Apple Music запрещён |

## Артефакты (gate T3 закрыт 7/7)

`openspec/changes/tempo-program/` — 9 файлов, `openspec validate` проходит:

- `proposal.md` — почему, таблица волн, модель данных, риски
- `manifest.md` — 46 требований R01–R46 с дословными цитатами
- `tasks.md` — 107 задач по 13 волнам
- `interfaces.md` — 10 разделов контрактов (токены, DDL, Rust-команды, шорткаты, поиск, медиа, бюджеты)
- `recon.md` — инвентарь 20k строк + source-verified разведка
- `specs/{shell,pomodoro,daily-planning}/spec.md` — нормативные спеки (MUST/WHEN/THEN)
- `worktrees.md` — изоляция и ветки · `oracle-verdict.md` — вердикт независимой проверки

## Контракты, которые нельзя менять без владельца

- `src/constants/design.ts` + `src/components/ui/**` — владелец Wave 0; цвета только из токенов
- `src/services/db.ts` — **единственная** точка SQL; `EntityMeta` = `id` (UUID v4) + `updated_at` + `deleted_at`
- Слой шорткатов — односложные клавиши НЕ срабатывают в `input`/`textarea`/`contenteditable`
- Реестр источников поиска — волна-владелец регистрирует проекцию своей сущности
- Жизненный цикл медиа — удаление записи удаляет файл; потолок хранилища и очистка
- Бюджеты: фон ≤2% CPU в покое · холст 60 к/с при 500 штрихах · запись экрана ≤15% CPU · поиск ≤100 мс

## Разведка (проверено по исходникам, не по памяти)

- **Handy** (MIT): движки уже опубликованы крейтами — `transcribe-cpp` 0.2.3 (ggml/Vulkan) и
  `transcribe-rs` 0.4.0 (ONNX, CPU); хоткеи `handy-keys` 0.3.4; вставка через Windows `SendInput`
  с сохранением и восстановлением буфера обмена; захват `cpal` 16 кГц + VAD.
  Дефолт Handy — Parakeet EN 0.6B (661 МБ, **только английский**) → для русского берём Whisper.
- **Google**: loopback + PKCE (OOB отключён Google); `calendar.events`/`calendar.readonly` — sensitive,
  верификация требует домена, политики и видео; в статусе Testing refresh-токен живёт **7 дней**;
  `drive.file` — единственный non-sensitive scope для Drive; инкрементальный синк через `syncToken`,
  при `410 Gone` — полная пересинхронизация; вебхуки для desktop недоступны → опрос.

## Где мы сейчас

**Готово и проверено:** волны 0 (фундамент), 1 (помодоро + фон), 2 (поиск ⌘K),
3 частично (задачи, списки, поиск), 4 (Daily Planning + Task Rollover).
Версия 0.8.0 проставлена во всех трёх манифестах, portable-сборка проверена на живых
данных пользователя.

**Следующее:** 3.3–3.6 (Google Calendar) — **заблокировано на пользователе**, нужен
Cloud-проект. Экран дня уже читает таблицу `events`, поэтому события подключатся к
готовому экрану, без переделки. Пока блокер не снят — идём по волнам, не зависящим от
Google: 5 (Notes), 6 (Drawings), 7 (Recordings), 9 (Statistics), 10 (Settings),
11 (Speech to Text).

**Состояние репозитория:** `D:/WORK/Alarmer` · ветка `feat/wave0-foundation` ·
295 vitest + 77 cargo · clippy `-D warnings` чист · покрытие 67.81/58.35/63.38/69.01 ·
`master` не тронут (там рабочая 0.3.3). Отступление от буквы T3: worktree на волну,
а не на каждую из 13 (иначе 13 Rust-сборок по гигабайту) — обосновано в `worktrees.md`.

**Инструменты для пользователя:** `docs/ГАЙД-СБОРКА-И-ПРОВЕРКА.md` — сборка,
portable-копия, чек-лист проверки глазами, переезд с 0.3, диагностика.

## Открытые блокеры

1. **Google Cloud проект** — создаёт пользователь; нужен к волне 3 (Calendar).
2. **Коллизии имени Tempo** — не проверены (веб-поиск недоступен без подписки); решение пользователя.

## Волна 0 — ЗАВЕРШЕНА (кроме релиза)

Ветка `feat/wave0-foundation`, коммиты `e0ea3b4` → `d8e6a4e`. 14 из 15 задач готовы
и проверены; 0.11 (автообновление на живой portable-копии) ждёт вас.

**Гейт:** `tsc` 0 ошибок · eslint 0 проблем · 226 vitest · 81 cargo · покрытие
62.58/55.14/58.45/63.82 при порогах 53/50/50/54 · `tempo.exe` собирается ·
приложение проверено в браузере: все 6 экранов, смена акцента одной переменной,
односложные хоткеи не срабатывают в полях ввода · archmap: 0 высоких находок.

**Что нашли проверки, а не авторы кода** (все исправлено):
1. `App.tsx` возвращал мини-окно до своих хуков — нарушение порядка хуков.
2. `installShortcutLayer` не вызывал `registerDefaultShortcuts` — клавиатура мертва.
3. Вкладка «Основные» в настройках была кнопкой без содержимого.
4. `resetCache` не чистил кэш настроек — состояние сайдбара протекало между запусками.
5. `repo().all()`/`search()` возвращали `undefined` при отсутствии IPC-команды.
6. Мигратор звал несуществующую команду `load_legacy_store`.
7. Цикл зависимостей `aiCompiler → aiHistory → types` (создан реэкспортом).

**Осталось до релиза 0.4.0** (нужны вы):
- `bun run tauri build` → portable-архив, тег `v0.4.0`, проверить в приложении
  предложение обновления и подмену файла (R33).
- Одна ручная переустановка ломает автообновление у старой копии Alarmer — это
  ожидаемо и записано в решении R32.

## Волна 1 — ЗАВЕРШЕНА

Ветка `feat/wave0-foundation`, коммиты `4b1432a`, `8abb93c`. Задачи 1.1–1.9 готовы
и проверены. **Гейт:** tsc 0 · eslint 0 · 240 vitest · 74 cargo · clippy чист ·
покрытие 65.37/55.92/59.83/66.58 (пороги 53/50/50/54) · archmap 0 высоких находок.

Что сделано: режимы `pomodoro`/`stopwatch`, фазы `focus`/`short_rest`/`long_rest`;
перерыв стартует сам, `auto_start` управляет только следующим фокусом; длительности
10–120 мин; сессии пишет Rust в тик-цикле в таблицу `sessions` (только фазы фокуса);
экран с крупным циферблатом, номером помодоро и счётчиком за день; динамический фон
на CSS-переходе без цикла анимации; Focus Audio с четырьмя генерируемыми шумами.

**Что нашли проверки, а не авторы кода:**
1. Вкладка «Помодоро» в настройках была написана, но **не смонтирована** — до
   ползунков нельзя было добраться из интерфейса.
2. `PomodoroSettings` использовал несуществующие пропы `unit` (Slider) и `size`
   (Segmented) — контракт кит-компонентов заморожен в волне 0.
3. `QualityPrompt` остался без единого потребителя (мёртвый код удалённых
   направлений) — удалён вместе с тестом.
4. Отладочный `console.log` и `try/catch`, который только перебрасывал ошибку.
5. Путь записи сессий в БД не был покрыт тестом — добавлены три теста против
   настоящей SQLite, проверены мутацией.

## Волна 2 — ЗАВЕРШЕНА

Коммиты `d6d7e13`, `4afbeb9`. Задачи 2.0–2.5 готовы и проверены, кроме релиза 0.6.0.
**Гейт:** tsc 0 · eslint 0 (без предупреждений) · 267 vitest · 76 cargo · clippy чист ·
покрытие 66.86/57.01/61.63/68.07 · версия 0.6.0 проставлена.

Что сделано: Rust индексирует будильники и сессии в `search_fts`; запрос санируется
перед `MATCH` (каждый токен в кавычках, последний с `*`); `reindex` вызывается при
старте в `dbReady`; палитра ⌘K с группировкой, подсветкой совпадения, стрелками и
Enter; реестр команд (навигация, таймер, сайдбар, акцент).

**Что нашли проверки, а не авторы кода:**
1. **Ни один реестр не устанавливался** — палитра открывалась пустой. Добавлен
   `installSearchRegistry()` в `main.tsx` (идемпотентный: StrictMode монтирует дважды).
2. **Реестр кешировался через `useMemo([])`** — `main.tsx` наполняет его в эффекте,
   который идёт ПОСЛЕ первого рендера палитры, поэтому кеш навсегда оставался пустым.
3. Два `setState` синхронно в эффекте (каскадные рендеры) — результат поиска теперь
   хранится вместе со своим запросом, индекс строки зажимается при чтении.
4. Не хватало метки `changeAccent` — добавлена в оба языка до раздачи.

Санитайзер FTS доказан **мутацией**: сырой запрос роняет два теста.

## Волна 3 — ЧАСТИЧНО ЗАВЕРШЕНА

Коммиты `0ca214b`, `884984a`, `765d59e`, `9ef0906`. Сделаны **3.1, 3.2, 3.8**
(задачи, списки, поиск). Версия 0.7.0 проставлена.
**Гейт:** tsc 0 · eslint 0 · 295 vitest · 77 cargo · clippy чист ·
покрытие 67.81/58.35/63.38/69.01.

**ЖДЁТ ВАС — блокирует 3.3–3.6:** Google Cloud проект (OAuth client типа Desktop,
включённые Calendar API и Drive API, тестовые аккаунты в Audience). Без него
авторизация, синхронизация календаря и запись задач событиями не принимаются —
проверить их нечем.

**Что нашли проверки, а не авторы кода:**
1. **Мост молча терял все поля R07** — `taskToRow` писал нули в priority/due_date/
   start_at/planned_minutes, а `taskFromRow` читал их в `void`. Колонки были, UI их
   не видел, ничего не падало. Тест `store.test.ts` при этом **проверял, что поля
   теряются** — он закреплял дефект.
2. `AlarmRow` объявлял фантомный `duration_minutes`, которого нет в схеме будильников.
3. Метки сайдбара были зашиты по-русски — переключатель RU/EN не переводил навигацию.
4. Дашборд встраивал полноэкранный компонент в карточку виджета.
5. Агенты трижды выдумали пропсы у кит-компонентов (`Row.icon`, `Row.className`,
   `IconButton.title`, `IconButton.variant`) и два ключа i18n.
6. Палитра шла событие `tempo:reveal`, которое **никто не слушал** — переход к
   найденному не доводился. Теперь экран задач открывает группу выполненных,
   прокручивает строку и подсвечивает её.

## Волна 3б (после вашего Cloud-проекта): Google Calendar

Задачи 3.3–3.6 + приёмка. Разведка уже сделана: loopback + PKCE, `drive.file` —
единственный non-sensitive scope для Drive, в статусе Testing refresh-токен живёт
7 дней, при `410 Gone` — полная пересинхронизация.

## Волна 4 — ЗАВЕРШЕНА

Коммит `dc9e1cf`. Задачи 4.1–4.5, версия 0.8.0.
**Гейт:** tsc 0 · eslint 0 · 335 vitest · 77 cargo · clippy чист ·
покрытие 68.83/59.44/64.47/70.25 · archmap 0 высоких находок.

Что сделано: экран дня с тремя шагами (план / день / обзор) и часовым рельсом;
события читаются из таблицы `events`, поэтому Google-события появятся в готовом
экране после снятия блокера; перенос задач выключен по умолчанию, при включении
переносит незавершённое на сегодня и чистит устаревший слот; часовой пояс виден
рядом с настройкой.

**Что нашли проверки, а не автор кода:**
1. Заголовок даты брал локаль ОС, а не язык интерфейса — на английской Windows
   при русском интерфейсе показывалось «Sun, Sep 20». Теперь «вс, 20 сент.».
2. `setState` синхронно в эффекте в настройках переноса (каскадный рендер).
3. Мёртвая деструктуризация в тесте событий.
4. Идемпотентность переноса доказана **мутацией**: расширение условия с «раньше
   сегодня» на «раньше или сегодня» роняет два теста.

## Проверено на живых данных (2026-09-20)

Собрана portable-копия 0.7.0, в неё положен настоящий `alarmer.json` от установленной
0.3.3, приложение запущено. Результат: 3 сообщения копилота и 12 настроек перенесены,
программы отброшены, ключ не попал в базу.

**Найдены и исправлены два дефекта миграции, которые тесты не ловили:**
1. Файл обёрнут в `{ "state": ... }` плагином хранилища, а мигратор читал верхний
   уровень — находил ноль записей и **рапортовал об успехе**.
2. Легаси-сущности лежат в **канонической** форме (`title`, массив `days`, `done`),
   а мигратор прогонял их через чтение строк БД (`label`, `status`, `body_md`) —
   получались пустые записи. Тест использовал плоскую фикстуру с именами колонок,
   поэтому проходил, пока реальный файл не мигрировал ничего.

Плюс исправлен поиск файла: identifier сменился при переименовании, поэтому у
установленной копии файл лежит в каталоге старого идентификатора
(`%APPDATA%\com.alarmer.smart\`), а не рядом с новой базой.

## Осталось за вами

1. **Google Cloud проект** — блокирует 3.3–3.6.
2. **Релизы 0.4.0 … 0.7.0**: сборка установщика требует приватный ключ подписи
   (в GitHub Secrets); локально собирайте с `--no-bundle`, а релиз делает CI по тегу.
   Проверка автообновления — шаг 7 гайда.
