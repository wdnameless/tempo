# Проверка: видимость диктовки (2026-09-24)

Verdict: ACCEPT

## Четыре дыры, найденные чтением кода, и их закрытие

| # | Дыра | Как закрыто |
|---|---|---|
| 1 | Ошибка диктовки показывалась только внутри раздела STT, хотя диктуют из другого окна | Плашка поверх всех экранов: `SpeechPanel.tsx:281` больше не единственное место |
| 2 | `stt://hotkey-error` (система не отдала клавишу) не слушал никто | Событие в типизированном объединении + подписка; продюсер без потребителя теперь ошибка типов |
| 3 | `stt://vad-fallback` (нейродетектор деградировал) не слушал никто | То же |
| 4 | Индикатор исчезал на отпускании, хотя движок ещё считал: медленная модель выглядела как сломанная | Состояние «Распознаём…» до результата или ошибки |

## Проверено запуском

- `bunx vitest run`: 814 тестов (было 780) — плашка на каждое из четырёх событий, закрытие кликом,
  неизвестное событие не роняет, индикатор держится в фазе обработки и уходит по результату и по ошибке.
- `cargo test`: 297; `tsc` и `clippy` чисто.
- Приложение 0.23.12 запущено: процесс жив, диагностический лог пуст — значит клавиша встала.

## Что осталось непроверяемым

Плашка поверх **чужого** окна (в другом приложении) — межоконное поведение нативного окна автоматика не
воспроизводит: проверено рендерингом и изоляцией фокуса (`tabIndex={-1}`), но не глазами на чужом окне.
