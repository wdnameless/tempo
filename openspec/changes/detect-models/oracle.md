# Проверка: обнаружение чужих моделей и запуск GGUF (2026-09-24)

Verdict: ACCEPT

Проверено запуском на этой машине, а не чтением кода.

## Что проверено

| Что | Как | Результат |
|---|---|---|
| Скан находит чужую модель | прогон каталога на реальной машине | `Nemotron 3.5 Asr Streaming 0.6b Q8 0 \| engine=transcribecpp \| source=detected \| origin=Some("Handy") \| deletable=Some(false) \| 716 MB` — путь указывает прямо в кэш HF |
| Модель запускается на месте | тот же файл, русский клип | arch `parakeet`, variant `nemotron-3.5-asr-streaming-0.6b`, 32 языка включая `ru-RU`, streaming; услышано: «Привет! Эта проверка распознавания русской речи. Сегодня хорошая погода, и мы тестируем движок.» |
| Дубликаты схлопываются | тест: один файл через два кэша | одна запись |
| Отсутствующий каталог не роняет скан | тест | проходит |
| Чужое нельзя удалить | тест и прогон | `deletable: false` |
| Своё по-прежнему удаляется | тест | кнопка удаления есть только у своих |

## Находка: мёртвые ссылки в кэше

В кэше HuggingFace лежат три репозитория Handy, но у `parakeet-tdt-0.6b-v2` и `v3` в `snapshots/`
остались только симлинки — **сами блобы удалены**. Мёртвая ссылка в списке не появляется: предлагать
модель, которая не откроется, хуже, чем не показывать её. В списке оказался только Nemotron — тот,
чей файл на диске действительно есть.

## Что осталось непроверяемым

Живой микрофон; распознавание длинной речи. Проверено на синтезированных клипах, где эталон известен.
